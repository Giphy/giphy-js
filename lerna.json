{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["ignored-file", "*.md"],
            "allowBranch": ["master", "moat-loader"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
