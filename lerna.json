{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["ignored-file", "*.md"],
            "allowBranch": ["master", "fix/react-components-search-bar-input"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
