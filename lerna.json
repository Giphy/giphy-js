{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["ignored-file", "*.md"],
            "allowBranch": ["master"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
