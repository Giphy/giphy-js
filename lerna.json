{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["ignored-file", "*.md"],
            "allowBranch": ["master", "feat/search-bar"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
