{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "ignoreChanges": ["ignored-file", "*.md"],
            "allowBranch": ["master", "moat-beta"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
